// project imports
import CheckMail from 'views/pages/authentication/authentication1/CheckMail1';

// ================================|| CHECK MAIL ||================================ //

const CheckMailPage = () => {
  return <CheckMail />;
};

export default CheckMailPage;
