// project imports
import CheckMail from 'views/pages/authentication/authentication2/CheckMail2';

// ================================|| CHECK MAIL ||================================ //

const CheckMailPage = () => {
  return <CheckMail />;
};

export default CheckMailPage;
