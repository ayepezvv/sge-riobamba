// project imports
import CheckMail from 'views/pages/authentication/CheckMail';

// ================================|| CHECK MAIL ||================================ //

const CheckMailPage = () => {
  return <CheckMail />;
};

export default CheckMailPage;
