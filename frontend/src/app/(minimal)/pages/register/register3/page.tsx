// project imports
import Register from 'views/pages/authentication/Register';

// ================================|| REGISTER ||================================ //

const RegisterPage = () => {
  return <Register />;
};

export default RegisterPage;
