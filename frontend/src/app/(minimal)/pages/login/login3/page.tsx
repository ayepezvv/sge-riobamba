// project imports
import Login from 'views/pages/authentication/Login';

// ================================|| LOGIN ||================================ //

const LoginPage = () => {
  return <Login />;
};

export default LoginPage;
